## [v0.2.4] - 2026-05-04

### 技术改进/重构 (Changed/Refactor)
* 将 Rust CLI package version 提升到 `0.2.4`，使 `FlipBits version` 的 `presentation` 输出、Cargo package metadata 和 CLI presentation history 对齐。
* 明确 CLI crate 版本与 libs/core 版本分离：CLI 版本继续由 `apps/audio_cli/rust/Cargo.toml` 管理，core 版本继续由根仓 CMake 版本配置管理。
