## [v0.1.1] - 2026-03-08

### 技术改进/重构 (Changed/Refactor)
* 重构 `audio_cli` 依赖关系，改为仅链接 `bag_api` 与 `audio_io`，不再直接依赖 `audio_core` C++ 头文件。
* 统一 `version` / `--version` 输出中的表现层版本为 `0.1.1`，并通过 `bag_core_version()` 读取内核版本。

### 修复 (Fixed)
* 修复 CLI 编码/解码链路对重复实现的依赖，统一改为走稳定 C API 与 `wav_io`。
