# audio_cli v0.1.0 更新说明

发布日期：2026-03-01

## 版本信息
- 表现层版本：`0.1.0`
- 适用应用：`audio_cli`

## 变更内容
- 新增 `version` / `--version` 命令。
- 统一显示两种版本号：
  - 表现层版本（CLI）：`0.1.0`
  - 内核版本（Core）：通过 `bag::CoreVersion()` 自动读取。

## 兼容性
- 不影响既有音频编码/解码能力。
- 仅新增版本查询与展示输出。
