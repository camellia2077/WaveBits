## [v0.1.0] - 2026-03-01

### 新增功能 (Added)
* 新增 `bag::CoreVersion()` 与 `bag_core_version()` 版本查询接口。

### 技术改进/重构 (Changed/Refactor)
* 统一由内核维护版本字符串，并向 CLI/Android 提供只读查询入口。
* 明确 `libs/audio_core` 与 `libs/audio_api` 的版本归属与对外暴露方式。
