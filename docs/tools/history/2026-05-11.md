## 2026-05-11

### 技术改进/重构 (Changed/Refactor)
* 统一 Android translation 工具的默认产物目录，旧口径分散写入 `temp/ai_translation_reviews`、`temp/translation_key_alignment_reports`、`temp/xml_text_checks` 与工具局部 `temp/agent_jobs`，新口径统一收口到项目根 `temp/translations/` 下的 `reviews`、`key_alignment`、`xml_dump` 和 `lint-baseline.json`。
* 更新 `tools/repo_tooling/android_translate/AGENTS.md` 与命令默认参数，明确生成与读取都从 `temp/translations/...` 走，不再使用工具私有 temp 目录作为默认工作区。
* 调整 Android Gradle 对 translation lint baseline 的接线，旧口径读取 `tools/repo_tooling/android_translate/lint-baseline.json`，新口径改为 `temp/translations/lint-baseline.json`，让构建校验与 `python tools/run.py android-translate` 的默认目录保持一致。

### 弃用/删除 (Deprecated/Removed)
* 删除 `tools/scripts/android/translate/temp/agent_jobs/style_probe/...` 下误留在仓库中的翻译调试产物，避免临时 review/task 文件继续进入版本控制。
* 从 git 历史中移除 `tools/scripts/android/translate/lint-baseline.json` 与 `tools/repo_tooling/android_translate/lint-baseline.json`，停止追踪不应进入仓库历史的 translation lint baseline 文件。
